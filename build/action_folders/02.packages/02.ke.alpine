# kafka-eagle build Packages

declare -a OPTS=( '--virtual' '.buildDependencies' )
declare -a PKGS=( git 
                  maven
                  tar
                  unzip 
                  wget )
